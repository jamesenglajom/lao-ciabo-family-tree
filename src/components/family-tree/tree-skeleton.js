export function TreeSkeleton() {
  return (
    <div className="flex h-full w-full animate-pulse items-center justify-center gap-3 text-xs font-medium uppercase tracking-wide text-ink-faint">
      <span className="h-2 w-2 rounded-full bg-accent" />
      Loading tree engine
    </div>
  );
}
